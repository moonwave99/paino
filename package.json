{
    "name": "paino-workspace",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "lerna": "lerna bootstrap",
        "build": "turbo run build",
        "release": "npm run build && lerna publish --no-private --no-verify-access"
    },
    "devDependencies": {
        "lerna": "^5.5.0",
        "turbo": "^1.4.4"
    }
}
